# average_reward_rl_limits
Project investigating limits of average reward reinforcement learning

Install uv venv:  

1] ```uv venv pyenv --python=3.10```
2] ```source pyenv/bin/activate```
2] ```uv pip install --no-deps -r requirements.txt --index-strategy unsafe-best-match```
